/**
 * GSAP 3.12.2 - Minified Version
 * NOTA: Este es un archivo de terceros.
 * Descarga la versión oficial desde: https://greensock.com/gsap/
 * 
 * Alternativa CDN:
 * <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
 */

// Archivo placeholder - Reemplazar con la versión oficial de GSAP
// El archivo real contiene ~100KB de código minificado

console.warn('gsap.min.js es un placeholder. Descarga la versión oficial desde greensock.com');

// Versión mínima para evitar errores
if (typeof window !== 'undefined' && !window.gsap) {
    window.gsap = {
        to: function () {
            console.error('GSAP no está cargado. Usa la versión oficial.');
            return { kill: function () { } };
        },
        from: function () {
            console.error('GSAP no está cargado. Usa la versión oficial.');
            return { kill: function () { } };
        },
        fromTo: function () {
            console.error('GSAP no está cargado. Usa la versión oficial.');
            return { kill: function () { } };
        },
        timeline: function () {
            console.error('GSAP no está cargado. Usa la versión oficial.');
            return { to: function () { return this; }, play: function () { return this; } };
        },
        set: function () {
            console.error('GSAP no está cargado. Usa la versión oficial.');
            return { kill: function () { } };
        },
        killTweensOf: function () { },
        delayedCall: function () {
            return { kill: function () { } };
        },
        registerPlugin: function () { },
        core: {
            Animation: function () { },
            Tween: function () { },
            Timeline: function () { }
        },
        easing: {
            Power1: {},
            Power2: {},
            Power3: {},
            Power4: {},
            Linear: {},
            Sine: {},
            Expo: {},
            Circ: {},
            Elastic: {},
            Back: {},
            Bounce: {},
            SteppedEase: function () { return function () { }; }
        },
        plugins: {}
    };
}