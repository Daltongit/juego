/**
 * Phaser 3.60.0 - Minified Version
 * NOTA: Este es un archivo de terceros. 
 * Descarga la versión oficial desde: https://phaser.io/download/stable
 * 
 * Alternativa CDN:
 * <script src="https://cdn.jsdelivr.net/npm/phaser@3.60.0/dist/phaser.min.js"></script>
 */

// Archivo placeholder - Reemplazar con la versión oficial de Phaser
// El archivo real contiene ~500KB de código minificado

console.warn('phaser.min.js es un placeholder. Descarga la versión oficial desde phaser.io');

// Versión mínima para evitar errores
if (typeof window !== 'undefined' && !window.Phaser) {
    window.Phaser = {
        Game: function () {
            console.error('Phaser no está cargado. Usa la versión oficial.');
        },
        Scene: function () { },
        GameObjects: {},
        Physics: {},
        Input: {},
        Loader: {},
        Sound: {},
        Textures: {},
        Animations: {},
        Math: {},
        Geom: {},
        Utils: {}
    };
}